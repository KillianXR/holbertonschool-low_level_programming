#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*
* char - function 
*/

int main(void)
{	
	char alphabet

	for (alphabet = 'a'; alphabet <= 'z'; low++)
		putchar(low);
		putchar('\n');

	return (0);
}
